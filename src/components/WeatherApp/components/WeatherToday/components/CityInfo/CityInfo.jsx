import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  flex: 1;
`;

const CityInfo = () => {
  return <Wrapper>sydney</Wrapper>;
};

export default CityInfo;
